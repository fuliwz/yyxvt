// Centralized friendly links configuration. Replace/add entries as needed.
// Keys are hostnames; nested/subdomains can reuse the same host-matching logic.
const FRIEND_LINKS = {
  // Example:
  // 'example.com': [
  //   { name: '示例导航', url: 'https://example.com/' }
  // ]
}

export default FRIEND_LINKS
